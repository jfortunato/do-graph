{
  "name": "do-graph",
  "version": "1.0.0",
  "main": "src/index.js",
  "author": "developer@jfortunato.com",
  "license": "MIT",
  "dependencies": {
    "chart.js": "^4.4.2"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  },
  "scripts": {
    "build": "esbuild src/index.js src/index.html --bundle --minify --sourcemap --loader:.html=copy --outdir=dist",
    "watch": "esbuild src/index.js src/index.html --bundle --sourcemap --loader:.html=copy --outdir=dist --watch",
    "clean": "rm -rf dist"
  }
}
